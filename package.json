{
  "name": "@alan-eicker/gh-package-demo",
  "version": "1.10.4",
  "description": "Github Package demo",
  "main": "index.js",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "prerelease:alpha": "npm version prerelease --preid=alpha && git push --follow-tags",
    "prerelease:beta": "npm version prerelease --preid=beta && git push --follow-tags",
    "prerelease:rc": "npm version prerelease --preid=rc && git push --follow-tags"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^11.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^9.2.6",
    "@semantic-release/npm": "^11.0.3",
    "@semantic-release/release-notes-generator": "^12.1.0",
    "semantic-release": "^22.0.12"
  }
}
